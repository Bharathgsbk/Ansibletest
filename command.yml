---
  - name: Execute Shell
    hosts: webservers
    tasks:

    - name : Shell command
      shell:
            "cat password"
         
    - name : Shell command
      shell:
            "history"
